Goals:
Learn how to use GitHub (branches, issues, commits, pull requests).
Practice creating and managing a simple GitHub repository.

Needs:
A GitHub repository that is public.

Step:
Create a new public GitHub repository.
Create a new branch(named 'other').
Open a pull request to merge the `other` branch into `main`.
 Create two issues, assign labels, and close them using commits
